#include <iostream>
using namespace std;
int main()
{
    int p, q, i, lcm;
    cout << "Enter two numbers: ";
    cin >> p >> q;
    for (i = (p > q ? p : q); ; i++)
    {
        if (i % a == 0 && i % q == 0)
        {
            lcm = i;
            break;
        }
    }
    cout << "LCM = " << lcm;
    return 0;
}
