#include <iostream>
using namespace std;
int main()
{
    int a;
    cout << "Enter the number of rows: ";
    cin >> a;
    for (int r = 1; r <= a; r++)
    {
        for (int c = 1; c <= r; c++)
        {
            cout << "* ";
        }
        cout << endl;
    }
    return 0;
}
