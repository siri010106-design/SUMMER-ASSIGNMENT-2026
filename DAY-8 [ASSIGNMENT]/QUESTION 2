#include <iostream>
using namespace std;
int main()
{
    int a;
    cout << "Enter the number of rows: ";
    cin >> a;
    for (int i = 1; i <= a; i++)
    {
        for (int k = 1; k <= i; k++)
        {
            cout << k;
        }
        cout << endl;
    }
    return 0;
}
