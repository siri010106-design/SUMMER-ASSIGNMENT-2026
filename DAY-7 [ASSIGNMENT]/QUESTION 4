#include <iostream>
using namespace std;
int a= 0;
int b(int n)
{
    if (n == 0)
        return a;
    a = a * 10 + (n % 10);
    return b(n / 10);
}
int main()
{
    int value;
    cout << "Enter a number: ";
    cin >> value;
    cout << "Reverse number = " << b(value);
    return 0;
}
