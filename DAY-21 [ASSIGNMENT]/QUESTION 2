#include<iostream>
using namespace std;
int main()
{
    char str[50];
    int i, n = 0;
    cout<<"Enter string: ";
    cin>>str;
    while(str[n] != '\0')
    {
        n++;
    }
    for(i = n-1; i >= 0; i--)
    {
        cout<<str[i];
    }
    return 0;
}
