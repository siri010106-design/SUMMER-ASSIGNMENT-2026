#include <iostream>
using namespace std;
int main()
{
    int i, count = 0;
    cout << "Enter a number: ";
    cin >> i;
    while (i> 0)
    {
        if (i % 2 == 1)
        {
            count++;
        }
        i = i/ 2;
    }
    cout << "Set bits = " << count;
    return 0;
}
